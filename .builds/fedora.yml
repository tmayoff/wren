image: fedora/40

sources:
    - https://git.sr.ht/~tmayoff/wren
tasks:
    - deps: |
        sudo dnf install -y just cmake ninja-build clang \
            catch2-devel libshaderc-devel \ 
            vulkan-headers vulkan-loader mesa-vulkan-drivers \ 
            spdlog-devel SDL2-devel expected-devel
    - build: |
        cd wren
        mkdir build
        cd build
        just configure
        just build
