image: fedora/40

sources:
    - https://git.sr.ht/~tmayoff/wren
tasks:
    - deps: |
        sudo dnf install -y just cmake ninja-build
    - build: |
        cd wren
        mkdir build
        cd build
        just configure
        just build
