image: fedora/39

sources:
    - https://git.sr.ht/~tmayoff/wren
tasks:
    - deps: |
        sudo dnf install just cmake ninja-build
    - build: |
        cd wren
        mkdir build
        cd build
        just configure
        just build
